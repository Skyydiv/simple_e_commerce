# Commands recap

This file is to recap all the commands and explain them alongside their arguments.

## Docker
### Database
utiliser plutôt docker compose avec un fichier de configuration plutôt que docker run 